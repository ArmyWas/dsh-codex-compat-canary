{
  "oneOf": [
    { "properties": { "method": { "type": "string", "enum": ["item/commandExecution/requestApproval"] } } },
    { "properties": { "method": { "type": "string", "enum": ["item/fileChange/requestApproval"] } } }
  ]
}
